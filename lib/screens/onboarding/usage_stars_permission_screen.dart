import 'package:flutter/widgets.dart';

class UsageStatsPermissionScreen extends StatelessWidget {
  const UsageStatsPermissionScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
